import {articles} from '../../stories';
import {notFound} from 'next/navigation';
import {pageMeta,origin,jsonLd} from '../../seo';
export function generateStaticParams(){return articles.map(a=>({slug:a.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=articles.find(a=>a.slug===slug);if(!a)return {title:'Story not found | JUWA3X'};const meta=pageMeta(a.title+' | JUWA3X',a.description,'/journal/'+slug,a.image);return {...meta,openGraph:{...meta.openGraph,type:'article',authors:['JUWA3X Editorial']}}}
export default async function Article({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=articles.find(a=>a.slug===slug);if(!a)notFound();const related=articles.filter(x=>x.slug!==slug).slice(0,2);return <main className="article wrap"><nav className="breadcrumbs" aria-label="Breadcrumb"><a href="/">Home</a> / <a href="/blog">Blog</a> / <span>{a.category}</span></nav><p className="eyebrow">{a.category} / {a.read} MIN READ</p><h1>{a.title}</h1><p className="dek">{a.description}</p><p className="byline">By JUWA3X Editorial · Sources reviewed September 21, 2026</p><img className="article-hero" src={a.image} alt={a.alt}/><div className="article-body">{a.sections.map(([title,body])=><section key={title}><h2>{title}</h2><p>{body}</p></section>)}<aside>Sources: <a href="https://www.juwa777.com/games" rel="noreferrer" target="_blank">Juwa777 games page</a> and the supplied Juwa website’s category list and original artwork. Design observations are JUWA3X commentary. This independent publication does not verify gameplay, payouts or current availability. <a href="/contact">Editorial corrections & website support</a>.</aside><p><a className="read-link" href="/game-worlds">Explore the full game gallery & free mini-games →</a></p><h2>Keep exploring</h2>{related.map(r=><p key={r.slug}><a className="read-link" href={'/journal/'+r.slug}>{r.title} →</a></p>)}<a className="button" href="/blog">Back to all stories →</a></div><script type="application/ld+json" dangerouslySetInnerHTML={{__html:jsonLd({'@context':'https://schema.org','@type':'BlogPosting',headline:a.title,description:a.description,image:origin+a.image,mainEntityOfPage:origin+'/journal/'+a.slug,author:{'@type':'Organization',name:'JUWA3X Editorial',url:origin+'/about'},publisher:{'@type':'Organization',name:'JUWA3X',url:origin},dateModified:'2026-09-21'})}}/><script type="application/ld+json" dangerouslySetInnerHTML={{__html:jsonLd({'@context':'https://schema.org','@type':'BreadcrumbList',itemListElement:[{'@type':'ListItem',position:1,name:'Home',item:origin},{'@type':'ListItem',position:2,name:'Blog',item:origin+'/blog'},{'@type':'ListItem',position:3,name:a.title,item:origin+'/journal/'+a.slug}]})}}/></main>}
