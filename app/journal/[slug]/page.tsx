import {articles} from '../../stories';
import {notFound} from 'next/navigation';
export function generateStaticParams(){return articles.map(a=>({slug:a.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=articles.find(a=>a.slug===slug);return {title:a?`${a.title} | Juwa Afterdark`:'Story not found',description:a?.description}}
export default async function Article({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=articles.find(a=>a.slug===slug);if(!a)notFound();return <><header className="wrap header"><a className="brand" href="/">juwa<span>✦</span><small>AFTERDARK</small></a><a href="/#journal">← Back to the journal</a></header><main className="article wrap"><p className="eyebrow">{a.category} / {a.read} MIN READ</p><h1>{a.title}</h1><p className="dek">{a.description}</p><img className="article-hero" src={a.image} alt={a.alt}/><div className="article-body">{a.sections.map(([title,body])=><section key={title}><h2>{title}</h2><p>{body}</p></section>)}<aside>Editorial basis: artwork supplied with the Juwa project and the game-category descriptions on the <a href="https://www.juwa777.com/" rel="noreferrer" target="_blank">Juwa homepage</a>. No current catalog, payout, or popularity claims are implied.</aside><a className="button" href="/#journal">Back to all stories →</a></div></main><footer className="wrap"><span>JUWA AFTERDARK / THE JOURNAL</span><a href="/">Home ↑</a></footer></>}
